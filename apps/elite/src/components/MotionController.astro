<script>
  const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const header = document.querySelector<HTMLElement>(".site-header");

  const updateHeader = () => {
    if (!header) return;
    header.classList.toggle("is-scrolled", window.scrollY > 12);
  };

  updateHeader();
  window.addEventListener("scroll", updateHeader, { passive: true });

  if (!reducedMotion && "IntersectionObserver" in window) {
    const sections = Array.from(
      document.querySelectorAll<HTMLElement>("main > section, main > div > section")
    );

    const itemSelector = [
      ".visitor-path-card",
      ".home-new-strip-card",
      ".trust-signal",
      ".home-style-card",
      ".style-poster-card",
      ".studio-world-card",
      ".coach-card",
      ".faq-item",
      ".price-card",
      ".level-line-item",
      ".training-format-wide",
      ".intensive-roadmap-step",
      ".rental-room-card",
      ".advanced-card",
      ".advanced-format-band-v3 a",
      ".contact-choice-v3 a",
      ".trial-fact"
    ].join(",");

    const imageSelector = [
      ".hero-video",
      ".visitor-path-guided img",
      ".home-style-card img",
      ".trial-media img",
      ".studio-world-card img",
      ".service-page-media img",
      ".pricing-page-media img",
      ".rental-room-card img",
      ".rental-hero-v3 img",
      ".advanced-card img",
      ".advanced-hero-v3 img",
      ".about-cinematic-v3 img",
      ".about-editorial-v3 img",
      ".contact-hero-v3 img"
    ].join(",");

    document.documentElement.classList.add("motion-ready");

    sections.forEach((section, index) => {
      let mode = ["rise", "editorial", "soft"][index % 3];
      if (section.classList.contains("home-hero")) mode = "hero";
      if (section.matches(".home-style-showcase, .rental-gallery-v3, .advanced-offer-v3")) mode = "gallery";
      if (section.matches(".about-cinematic-v3, .about-editorial-v3, .contact-hero-v3, .rental-hero-v3, .advanced-hero-v3")) mode = "cinematic";
      if (section.matches(".schedule-v3, .schedule-page-hero-v3")) mode = "schedule";
      if (section.matches(".intensive-roadmap-panel")) mode = "timeline";

      section.dataset.reveal = mode;

      section.querySelectorAll<HTMLElement>(itemSelector).forEach((item, itemIndex) => {
        item.dataset.revealItem = "";
        item.style.setProperty("--reveal-index", String(Math.min(itemIndex, 8)));
      });

      section.querySelectorAll<HTMLElement>(imageSelector).forEach((media) => {
        media.dataset.motionImage = "";
      });
    });

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;
          entry.target.classList.add("is-visible");
          observer.unobserve(entry.target);
        });
      },
      { rootMargin: "0px 0px -6%", threshold: 0.08 }
    );

    sections.forEach((section) => observer.observe(section));
  }
</script>
